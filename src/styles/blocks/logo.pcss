.logo {
  margin-bottom: 60px;
  padding-left: 2.5rem;
}

.logo__icon {
  width: 50px;
  height: 50px;
  margin-right: 10px;
}

.logo__text {
  text-transform: uppercase;
}

.logo__link {
  text-decoration: none;
  font-family: $ffPoppins;
  font-weight: 500;
  display: flex;
  align-items: center;
}

