.cards {
  font-family: $ffRoboto;
}

.cards__list {
  display: flex;
  flex-wrap: wrap;
  margin-left: -20px;
}

.cards__item {
  min-width: calc(100% / 3 - 20px);
  margin-left: 20px;
  margin-bottom: 15px;

  @include tablets {
    min-width: calc(100% / 2 - 20px);
  }
}